- clean: ['~']
- link: 
    ~/.vimrc: .vimrc
- shell:
    - mkdir -p ~/.ssh
    - cp .ssh/authorized_keys ~/.ssh/authorized_keys 
    - chmod 744 ~/.ssh
    - chmod 644 ~/.ssh/* -R
    - chmod 600 ~/.ssh/id_rsa
