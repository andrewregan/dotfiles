- clean: ['~']
- link: 
    ~/.vimrc: .vimrc
- shell:
    - mkdir -p ~/.ssh
    - cp .ssh/authorized_keys ~/.ssh/authorized_keys 
    - chmod g-rwx ~/.ssh -R
    - chmod o-rwx ~/.ssh -R
    - chmod u+rwx ~/.ssh -R
